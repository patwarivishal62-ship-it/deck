# Sample GitHub Actions workflow — copy to .github/workflows/build-android-apk.yml
# on a branch that allows workflow files, or run it locally with JDK 17 + Android SDK.
#
# name: Build Android APK
# on: [workflow_dispatch]
#
# jobs:
#   apk:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-java@v4
#         with: { distribution: temurin, java-version: "17" }
#       - uses: android-actions/setup-android@v3
#       - uses: gradle/actions/setup-gradle@v4
#         with: { gradle-version: "8.7" }
#       - run: gradle assembleRelease --no-daemon
#         working-directory: android
#       - run: mkdir -p client/public/downloads && cp android/app/build/outputs/apk/release/app-release.apk client/public/downloads/deck.apk
