export const metadata = {
  title: "Dashboard",
  description: "Your DECK overview — projects, progress, and today's tasks at a glance.",
};

export default function DashboardLayout({ children }) {
  return children;
}
