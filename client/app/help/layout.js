export const metadata = {
  title: "Help & Guidebook",
  description: "A detailed DECK guidebook — how every screen works, plus answers to common questions.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function HelpLayout({ children }) {
  return children;
}
