export const metadata = {
  title: "Analytics",
  description: "Detailed project analytics for your DECK workspace — status, progress, tasks, and trends.",
  robots: {
    index: false,
    follow: false,
  },
};

export default function AnalyticsLayout({ children }) {
  return children;
}
