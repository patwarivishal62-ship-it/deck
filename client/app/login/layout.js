export const metadata = {
  title: "Sign In",
  description:
    "Sign in to your DECK workspace. Plan campaigns, track marketing goals, and manage tasks — all in one minimal control center.",
  alternates: {
    canonical: "/login",
  },
  robots: {
    index: true,
    follow: true,
  },
};

export default function LoginLayout({ children }) {
  return children;
}
